<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.cart">

	<insert id="insert">
		INSERT INTO cart(cart_no, member_id, item_no, cart_quantity, cart_price, money) 
		VALUES
		(seq_cart_no.NEXTVAL, #{member_id}, #{item_no}, #{cart_quantity}, #{cart_price}, #{money})
	</insert>

	<select id="countCart">
		SELECT COUNT(*) FROM cart WHERE member_id = #{member_id} AND item_no = #{item_no}
	</select>	
	
	<update id="update">
		UPDATE cart 
		SET cart_quantity = cart_quantity + #{cart_quantity}, cart_price = cart_price + #{cart_price} 
		WHERE member_id = #{member_id} AND item_no = #{item_no} 
	</update>


</mapper>