<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.order">

	<insert id="insert">
		INSERT INTO package (order_no, member_id, item_no, order_quantity, order_price)
		VALUES
		(seq_order_no.NEXTVAL, #{member_id}, #{item_no}, #{order_quantity}, #{order_price})
	</insert>


</mapper>