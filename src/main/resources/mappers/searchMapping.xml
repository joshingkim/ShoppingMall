<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.search">
<insert id="insert">
INSERT INTO search (search_no, member_id, item_category, keyword)
VALUES (seq_search_no.NEXTVAL, #{member_id}, #{item_category}, #{keyword})
</insert>

<select id="searchlist" resultType="kr.co.domain.SearchVO">
SELECT * FROM search WHERE item_category = #{item_category} AND UPPER(keyword) LIKE '%' ||UPPER(#{keyword})|| '%'
</select>
</mapper>